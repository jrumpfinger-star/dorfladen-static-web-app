name: Azure Static Web Apps CI/CD (BESTELLSYSTEM)

on:
  push:
    branches:
      - feature/bestellsystem

jobs:
  build_and_deploy_bestellsystem:
    runs-on: ubuntu-latest
    name: Build and Deploy Bestellsystem
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BESTELLSYSTEM }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "static-site"
          api_location: "api"
          output_location: ""
